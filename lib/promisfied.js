// Generated by CoffeeScript 1.6.3
(function() {
  var Promise, pixiv, prmPixiv;

  pixiv = require("./pixiv");

  Promise = (require("es6-promise")).Promise;

  prmPixiv = {
    createSession: function(userId, password) {
      return new Promise(function(resolve, reject) {
        return pixiv.createSession(userId, password, function(error, session) {
          if (error) {
            reject(error);
          }
          return resolve(session);
        });
      });
    },
    getGrapichWork: function(session, workId) {
      return new Promise(function(resolve, reject) {
        return pixiv.getGrapichWork(session, workId, function(error, work) {
          if (error) {
            reject(error);
          }
          return resolve(work);
        });
      });
    },
    searchWorks: function(session, mode, order, pageNo, keyword) {
      return new Promise(function(resolve, reject) {
        return pixiv.searchWorks(session, mode, order, pageNo, keyword, function(error, works) {
          if (error) {
            reject(error);
          }
          return resolve(works);
        });
      });
    }
  };

  module.exports = prmPixiv;

}).call(this);
